# NUNCA versionar o arquivo .env real! Use este arquivo apenas como referência.
# Cada dev deve criar seu próprio .env localmente, com as variáveis sensíveis.

# Exemplo de arquivo de variáveis de ambiente para o backend (NestJS)
# Copie este arquivo para .env e preencha os valores conforme seu ambiente local

# Banco de dados
MONGODB_URI=

# JWT
JWT_SECRET=

# Porta do servidor
PORT=3000

# URL do frontend autorizado (para CORS)
CLIENT_URL=

# URLs para detecção de ambiente (IMPORTANTES!)
API_BASE_URL=
CLIENT_BASE_URL=
BACKEND_URL=

# E-mail transacional (Brevo ou SMTP)
BREVO_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_NAME=
FROM_EMAIL=
MAIL_FROM=

# Ambiente
NODE_ENV=development

# Super Admin (seed)
SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD=
SUPER_ADMIN_NAME=

# Railway (produção - configurar no painel do Railway)
RAILWAY_PUBLIC_DOMAIN=
RAILWAY_STATIC_URL= 